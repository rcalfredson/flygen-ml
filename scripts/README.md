# Scripts

This directory is reserved for one-off utilities that are useful during fixture preparation or validation.

Keep core loader and segmentation logic inside `src/flygen_ml/` rather than here.
